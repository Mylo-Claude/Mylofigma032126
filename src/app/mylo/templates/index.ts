import { defaultTemplate } from './default';
import { formalTemplate } from './traditional';
import { modernTemplate } from './modern';

export const availableTemplates = [
  defaultTemplate,
  formalTemplate,
  modernTemplate,
];

export { defaultTemplate, formalTemplate, modernTemplate };
