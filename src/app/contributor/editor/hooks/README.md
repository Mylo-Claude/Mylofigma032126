# Editor Hooks

This directory contains React hooks specific to the Editor panel.

## Future Hooks
- `useEditorCommands.ts` - ProseMirror command helpers
- `useFormatting.ts` - Formatting state and controls
- `useStructure.ts` - Heading and list controls
