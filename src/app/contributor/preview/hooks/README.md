# Preview Hooks

This directory contains React hooks specific to the Preview panel.

## Planned Hooks
- `usePreviewZoom.ts` - Zoom calculation and centering logic (Step 2.3)
- `usePageTracking.ts` - Scroll-based page tracking (Step 2.4)
